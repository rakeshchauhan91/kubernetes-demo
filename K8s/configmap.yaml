apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  APP_ENV: production
  LOG_LEVEL: info
  DB_HOST: host.docker.internal   # Reach host machine SQL Server
  DB_PORT: '1433'
  DB_NAME: Surveys
 